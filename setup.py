import os
import re
from setuptools import setup, find_packages

setup(
    name='equb',
    version='dev.0',
    description='',
    author='Henok Alem',
    packages=find_packages('.'),
    package_dir={'': ''}
)